# {{ ansible_managed }}
# Rendered by the docker_stack role from vault-backed variables.
# Edit the inventory / vault, not this file — it is overwritten on every run.
{% for key, value in (stack_env | default({})) | dictsort %}
{{ key }}={{ value }}
{% endfor %}
