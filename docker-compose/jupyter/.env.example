# JupyterLab + code-server stack — access secrets.
# Standalone: `cp .env.example .env`. Ansible renders it from vault/secrets.yml.
# Both services also sit behind Authelia SSO; these are defence-in-depth.
CODE_SERVER_PASSWORD=REPLACE-strong-password
JUPYTER_TOKEN=REPLACE-openssl-rand-hex-24
