# Traefik stack — environment.
# Standalone: `cp .env.example .env`. The Ansible `traefik` role renders .env
# automatically from vault variables. Traefik itself reads no secrets here;
# this file exists so the stack matches the other stacks' deploy pattern.
COMPOSE_PROJECT_NAME=traefik
